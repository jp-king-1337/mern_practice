# Types of data
type Task {
    _id: String
    text: String!
    username: String
    createdAt: String
    updatedAt: String
}

# Types of queries
type Query {
    getTasks: [Task]
    getTask(id: String): Task
}

# Types of mutations
type Mutation {
    addTask(username: String!, text: String!): Task
}